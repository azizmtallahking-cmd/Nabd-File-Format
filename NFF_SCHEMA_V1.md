
# NFF Schema Specification v1.0

هذه الوثيقة هي العقد الرسمي لبنية محتوى NFF وسياقات عرضه.

---

## 1. سياقات الحوكمة (Governance Contexts)

| السياق | صاحب القرار البصري | مدى الحرية | يتغير مع كل ملف؟ |
|---|---|---|---|
| **الويب المنفصل** | المستخدم | يختار من لوحات مُقرَّة سلفاً؛ ثابت عبر كل ملفاته. | لا - هوية شخصية ثابتة. |
| **نبض المندمج (HM)** | المرشد | حرية مُعلَّمة من مفردات مُدرَّبة حسب دلالة اللحظة. | نعم - لكن نادراً ومبرراً. |
| **AIM (مطلق)** | العقد الثابت | لا حرية إطلاقاً؛ بنية مجمّدة للتدريب الآلي. | لا أبداً. |

---

## 2. عقد AIM (مجمّد - Frozen)

يُحظر تعديل هذا القسم دون إصدار جديد صريح (`nff_version` يتغير). أي تغيير يتطلب إعادة تدريب واعية.

- **الهدف:** ضغط حقيقي للمعنى دون أي زخرفة بصرية.
- **القاعدة:** إزالة كتل `<nff-prose>`، تحويل QCM لصيغة نصية مضغوطة `[QCM:id] Q (v:l|...)`.
- **الترميز:** UTF-8 حصراً.

---

## 3. عقد HM (دلالي - Semantic)

يعتمد العرض على ترجمة **النية الدلالية** إلى تصميم حتمي عبر الكود، لا عبر إخراج كود حر من الذكاء.

### أنواع النبرة (Prose Tones)
- `executive`: جاد، رسمي، مركز.
- `narrative`: سردي، انسيابي، مريح.
- `urgent`: تحذيري، حساس زمنياً.
- `reflective`: تأملي، هادئ، مائل.
- `neutral`: النمط الافتراضي.

### أنواع التفاعل (QCM Types)
- `single_choice`: اختيار وحيد.
- `multi_choice`: خيارات متعددة.
- `text_input`: إدخال نصي.

---

## 4. هوية المستخدم (User Identity)

يتم دمج هوية المستخدم مع نية المحتوى لإنتاج العرض النهائي دون المساس بنواة NFF.

- **اللوحات (Palettes):** `amber-stone`, `sage-cream`, `indigo-slate`.
- **الخطوط:** `classic`, `modern`, `warm`.
- **الكثافة:** `comfortable`, `compact`.

---

## 5. بنية المستند (Document shape)

يبدأ جسم NFF بترويسة YAML مسطحة محاطة بـ `---`. يتبعها المحتوى المشفر بـ UTF-8.

```text
---
title: Example
priority: medium
classification: internal
---
<body>
```

يتم استخراج الوسوم الدلالية (`[[section]]`, `[[callout]]`, `[[priority]]`, `[[classification]]`, `[[tab]]`, `[[tag]]`) وتحليلها في شجرة دلالية موحدة.
