# NFF Integration Architecture

## 1. الهدف الأساسي: NFF كطبقة توافق وإخراج موحدة للذكاء الاصطناعي

الهدف من هذه المعمارية هو تحويل NFF من مجرد تطبيق لفتح الملفات إلى **معيار موحد للربط** يمكن لأي نموذج ذكاء اصطناعي (ChatGPT, Manus, Claude, إلخ) استخدامه لإنتاج ملفات `.nff` صحيحة. سواء تم الربط عبر Plugins، أو Skills، أو Actions، فإن التطبيق/النواة هي التي تقوم بالبناء الميكانيكي، بينما يكتفي الذكاء بتحديد المحتوى والنية الدلالية.

## 2. المبدأ الحاكم: فصل النية عن البايتات

لا يُسمح لأي نموذج ذكاء اصطناعي (LLM) بكتابة البايتات الـ 18 الخاصة برأس NFF مباشرة. النماذج اللغوية غير حتمية وقد تُفسد تسلسل البايتات الدقيق، مما ينقض مبدأ NFF كعقد ميكانيكي صارم.

**القاعدة المعمارية:**
- **الذكاء الاصطناعي يُقرر النية فقط:** يُخرج المحتوى، المود (AIM/HM)، والنبرات كـ JSON مهيكل حصراً.
- **محرك الترميز يبني الملف:** كود حتمي واحد (`core/generators.ts`) يستقبل الـ JSON ويبني ملف `.nff` بالبايتات الصحيحة 100% دائمًا.

## 3. التصميم المعماري: محرك واحد، ثلاث واجهات للربط

لضمان عدم انحراف التنفيذ عبر المنصات، تعتمد كل واجهات التكامل على نفس محرك الترميز الأساسي المكتوب بـ TypeScript:

### أ. واجهة التطبيقات (npm Package)
- **الهدف:** تكامل NFF داخل تطبيقات JS/TS أخرى.
- **التنفيذ:** فصل `core/` (بما فيها `generators.ts` و `parser.ts`) إلى حزمة مستقلة `@nabd/nff-core`.
- **الاستخدام:** تستورد التطبيقات الحزمة وتستدعي `generateAim()` أو `generateHm()` مباشرة.

### ب. واجهة ChatGPT (GPT Action / API)
- **الهدف:** تمكين ChatGPT من توليد ملفات NFF حقيقية للمستخدم.
- **التنفيذ:** نقطة API مستضافة (Serverless Function) تستقبل JSON من GPT، وتمرره إلى `@nabd/nff-core`، ثم تعيد ملف `.nff` كـ Downloadable Blob.
- **الاستخدام:** يُزوّد GPT بـ OpenAPI Schema يصف هيكل الـ JSON المطلوب، ويُمنع من محاولة كتابة الملف بنفسه.

### ج. واجهة الوكلاء المحليين (Local CLI / Skills)
- **الهدف:** تمكين وكلاء الذكاء المحليين (مثل Manus) من كتابة NFF دون الاعتماد على شبكة خارجية (مبدأ "محلي أولاً").
- **التنفيذ:** سكربت Node.js محلي صغير يستورد `@nabd/nff-core`، يقرأ JSON من `stdin` أو ملف مؤقت، ويكتب `.nff` إلى القرص.
- **الاستخدام:** يُوجّه الوكيل لكتابة نيته كـ JSON، ثم استدعاء السكربت لتوليد الملف النهائي.

## 4. هيكل عقد النية (Intent JSON Contract) للربط

يجب أن يلتزم أي نموذج ذكاء بهذا الهيكل عند طلب توليد NFF:

```json
{
  "mode": "HM", // أو "AIM"
  "frontmatter": {
    "title": "عنوان الملف",
    "tags": ["tag1", "tag2"]
  },
  "nodes": [
    {
      "type": "prose",
      "tone": "executive",
      "content": "نص الفقرة هنا."
    },
    {
      "type": "qcm",
      "question": "ما هو الخيار الصحيح؟",
      "qcmType": "single_choice",
      "options": [
        { "label": "الخيار الأول", "value": "opt1" },
        { "label": "الخيار الثاني", "value": "opt2" }
      ]
    }
  ]
}
```

## 5. خطة التنفيذ (Migration Sequence)

استناداً إلى `NFF_LIBRARY_PLAN.md`، فإن الخطوة التنفيذية الأولى لتحقيق هذا الربط هي فصل النواة الحالية إلى حزمة مستقلة:

1. **فصل النواة (Core Extraction):** استخراج `header.ts`, `model.ts`, `parser.ts`, و `generators.ts` إلى حزمة `@nabd/nff-core` دون تغيير ثوابت البايتات.
2. **اختبارات التطابق (Byte-for-byte tests):** التأكد من أن الحزمة الجديدة تولد نفس الرأس وتجتاز اختبارات Round-trip العربية.
3. **تحديث التطبيق:** استبدال الاستيرادات الداخلية في تطبيق NFF الحالي لتستخدم الحزمة الجديدة.
4. **نشر الواجهات:** بعد نجاح الحزمة، يتم بناء GPT Action و Local CLI فوقها لتمكين ChatGPT و Manus وباقي النماذج من الربط الفعلي.
